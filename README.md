# AI_Test

# Seq clustering
```text
| 파라미터          | 기본      | 언제 바꿈                                                          |
| ----------------- | --------- | ------------------------------------------------------------------ |
| `gap_minutes`     | `30`      | 세션을 더 좁게/넓게 보고 싶을 때                                   |
| `ngram_range`     | `(2,3,4)` | 5-gram까지 보거나 일부만 보고 싶을 때                              |
| `min_count`       | `5`       | 노이즈 컷오프. 데이터 작으면 1~2, 412k 전체면 10~20도 OK           |
| `collapse_repeats`| `True`    | 같은 시그니처 연속 실행(재실행)을 한 번으로 압축. False면 그대로   |
```
